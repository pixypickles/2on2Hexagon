const c=document.getElementById('game'),x=c.getContext('2d');const cx=c.width/2,cy=c.height/2,r=260;x.strokeStyle='#fff';x.lineWidth=4;x.beginPath();for(let i=0;i<6;i++){let a=Math.PI/6+i*Math.PI/3,X=cx+Math.cos(a)*r,Y=cy+Math.sin(a)*r;i?x.lineTo(X,Y):x.moveTo(X,Y)}x.closePath();x.stroke();x.fillStyle='dodgerblue';x.beginPath();x.arc(cx,cy+120,16,0,7);x.fill();x.fillStyle='gold';x.beginPath();x.arc(cx,cy,10,0,7);x.fill();